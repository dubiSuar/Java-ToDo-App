<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:background="@drawable/appbg"
    tools:context=".MainActivity">

    <!-- Shrink the logo size and reduce marginBottom -->
    <ImageView
        android:id="@+id/imageView"
        android:layout_width="120dp"
        android:layout_height="114dp"
        android:layout_marginBottom="8dp"
        app:srcCompat="@drawable/logowhite" />

    <!-- Reduce marginVertical of CardView -->
    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:cardCornerRadius="10dp"
        app:cardElevation="40dp"
        android:layout_marginHorizontal="24dp"
        android:layout_marginVertical="8dp"> <!-- Adjusted -->

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="24dp">

            <!-- Header -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Create an Account"
                android:textSize="30sp"
                android:textStyle="bold" />

            <!-- Subtext -->
            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="To be more productive!"
                android:textSize="12sp"
                android:textStyle="normal"
                android:paddingTop="-6dp"
                android:layout_marginBottom="16dp" />

            <!-- Username field -->
            <EditText
                android:id="@+id/reg_username"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Username"
                android:inputType="text"
                android:layout_marginBottom="16dp"
                android:padding="12dp"
                android:background="@drawable/input_bg"
                android:textColor="?attr/colorOnBackground"
                android:textSize="16sp" />

            <!-- First name field -->
            <EditText
                android:id="@+id/reg_firstname"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="First Name"
                android:inputType="text"
                android:layout_marginBottom="16dp"
                android:padding="12dp"
                android:background="@drawable/input_bg"
                android:textColor="?attr/colorOnBackground"
                android:textSize="16sp" />

            <!-- Last name field -->
            <EditText
                android:id="@+id/reg_lastname"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Last Name"
                android:inputType="text"
                android:layout_marginBottom="16dp"
                android:padding="12dp"
                android:background="@drawable/input_bg"
                android:textColor="?attr/colorOnBackground"
                android:textSize="16sp" />

            <!-- Password field -->
            <EditText
                android:id="@+id/reg_pword"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Password"
                android:inputType="textPassword"
                android:layout_marginBottom="16dp"
                android:padding="12dp"
                android:background="@drawable/input_bg"
                android:textColor="?attr/colorOnBackground"
                android:textSize="16sp" />

            <!-- Confirm Password field -->
            <EditText
                android:id="@+id/reg_cpword"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:hint="Confirm Password"
                android:inputType="textPassword"
                android:layout_marginBottom="16dp"
                android:padding="12dp"
                android:background="@drawable/input_bg"
                android:textColor="?attr/colorOnBackground"
                android:textSize="16sp" />

            <!-- Login button -->
            <Button
                android:id="@+id/btnRegister"
                android:layout_width="160dp"
                android:layout_height="60dp"
                android:text="Create Account"
                android:textStyle="bold"
                android:padding="16dp"
                android:backgroundTint="@color/purple"
                android:textColor="@color/white"
                android:layout_gravity="right" />

            <!-- Register link outside the card -->
            <TextView
                android:id="@+id/tvLogin"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center"
                android:layout_marginTop="16dp"
                android:clickable="true"
                android:gravity="center"
                android:text="Already Have an Account? Login Here"
                android:textSize="14sp"
                android:textColor="?attr/colorOnBackground"
                android:textStyle="bold" />

        </LinearLayout>

    </androidx.cardview.widget.CardView>

</LinearLayout>
